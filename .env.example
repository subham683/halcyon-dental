# When you're ready to replace the placeholder chat logic in
# lib/chat-responses.ts with a real model call, add your key here
# and reference it from app/api/chat/route.ts. Never commit real keys.
ANTHROPIC_API_KEY=
